import 'package:flutter/material.dart';

class GlobalImageNetworkLoadingWidget extends StatelessWidget {
  const GlobalImageNetworkLoadingWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
